<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ceasar.japmunchies">

    <application
        android:allowBackup="true"
        android:icon="@color/cardview_shadow_end_color"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ginger_chicken"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ChatActivity"
            android:windowSoftInputMode="stateHidden|adjustResize"></activity>
        <activity android:name=".Activities.SpecificRecipeStepActivity" />
        <activity android:name=".Activities.SpecificRecipe" />
        <activity android:name=".Activities.SignUpActivity" />
        <activity android:name=".Activities.SplashScreenActivity" />
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.MainActivity"
            android:windowSoftInputMode="stateHidden|adjustResize" />
    </application>

</manifest>